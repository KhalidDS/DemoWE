@model DemoWE.Models.Request
<head>

    <link rel="stylesheet" type="text/css" href="~/css/util.css">
    <link rel="stylesheet" type="text/css" href="~/css/main.css">
    <!--===============================================================================================-->
</head>
<form id="createForm" asp-action="Create">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <div class="wrap-input100 validate-input">
     
        <label asp-for="RequestTitle" class="control-label">Request Title</label>
        <input asp-for="RequestTitle" class="form-control" />
        <span asp-validation-for="RequestTitle" class="text-danger"></span>
    </div>

    <div class="wrap-input100 validate-input">
        <label asp-for="RequestDescription" class="control-label">Request Description</label>
        <input asp-for="RequestDescription" class="form-control" />
        <span asp-validation-for="RequestDescription" class="text-danger"></span>
    </div>

    <div class="wrap-input100 validate-input">
        <label asp-for="rfile" class="control-label">file</label>
        <input type="file" id="rfile" class="form-control" name="sfile" />
        <span asp-validation-for="rfile" class="text-danger"></span>
    </div>

    <!-- Hidden field for AssignedDepartmentID -->
    <input type="hidden" id="AssignedDepartmentID" name="AssignedDepartmentID" value="@ViewData["DepartmentID"]" />

    <div class="form-group">
        <input type="submit" value="Create" class="btn btn-primary" />
    </div>
</form>



@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

<script>
    function handleStartDate() {
        var checkbox = document.getElementById("startImmediately");
        var dateInput = document.getElementById("StartDate");

        if (checkbox.checked) {
            // Get the current system date in ISO 8601 format (year-month-day)
            var currentDate = new Date().toISOString().slice(0, 10);

            // Set the date input value to the current date
            dateInput.value = currentDate;
        } else {
            // Clear the date input value if unchecked
            dateInput.value = "";
        }
    }
</script>
<script>
    // Set AssignedDepartmentID to DepartmentID of the logged-in user
    document.getElementById("AssignedDepartmentID").value = "@ViewData["DepartmentID"]";
</script>